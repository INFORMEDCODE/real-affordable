type TextEntry = {};

const TextEntry: React.FC<TextEntry> = () => {
    return (
        <input
            className="border border-solid rounded p-2 border-black"
            type="text"
        />
    );
};

export default TextEntry;
